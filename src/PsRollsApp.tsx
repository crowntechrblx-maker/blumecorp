// PS C&M Rolls — cleared out for now. Was a full placeholder site (hero,
// practice areas, team, contact); content removed but the app window/icon
// stay in place so it can be rebuilt later.

export function PsRollsApp() {
  return <div className="app-content rolls-app" />;
}
